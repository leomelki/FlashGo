syntax = "proto3";

package animations;

import "animations/protos/configs/rainbow_config.proto";
message SetAnimation {
    AnimationType animation_type = 1;
    oneof config {
        configs.RainbowAnimationConfig rainbow_config = 2;
    }
}
enum AnimationType {
    RAINBOW = 0;
}